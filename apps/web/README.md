# Web inspector (Vertical Slice D)

Reserved for the dense timeline + inspector client. It will consume the same
event protocol as the CLI over WebSocket and will show context receipts,
capability page-ins, effect leases, and completion evidence. No client-specific
agent loop belongs here.
