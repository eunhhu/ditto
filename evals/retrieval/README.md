# Retrieval evaluation

The Rust test suite currently verifies that a 1,001-entry catalogue pages only
the relevant capability and never exceeds a six-schema epoch. Add Recall@k and
set-level composition fixtures here.
